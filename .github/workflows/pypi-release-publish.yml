name: ~python3 release publish
on:
  workflow_call:

jobs:
  release:
    uses: ./.github/workflows/release.yml@main
    permissions:
      contents: write
    secrets: inherit
  publish:
    needs: release
    uses: ./.github/workflows/pypi-publish.yml@main
    secrets: inherit
