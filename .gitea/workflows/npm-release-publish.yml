name: ~npm release publish
on:
  workflow_call:

jobs:
  release:
    uses: sanzenwin/workflows/.gitea/workflows/release.yml
    permissions:
      contents: write
    secrets: inherit
  publish:
    uses: sanzenwin/workflows/.github/workflows/npm-publish.yml
    secrets: inherit
    with:
      registry: $GITHUB_SERVER_URL/api/packages/$GITHUB_ACTOR/npm
